# just throwing out some ideas
# from trafficdata import get_data

# ev_density = ev_estimation_model(get_data()[traffic_density])
# charging_station_density = (run a simple select from the database of ev stations where euclidean distance <= our radius of interest) / area of interest
# should make some model that takes these parameters to calculate some optimal discount for buisnesses / charging rates 

